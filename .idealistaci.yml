info:
  name: docker-id-open-webui
  group: IDEALISTADATA
repository:
  format: none
  usage: none
jobs:
  jenkinsfile:
    - name: build-and-register-master
      file: jenkins/buildAndRegister
      upstream:
        scmBranch: master
        scm: true

    - name: build-and-register-dev
      file: jenkins/buildAndRegister
      upstream:
        scmBranch: dev
        scm: true